# Live Validation Notes

The revised pathway area now reads as an offset, numbered signal index rather than a uniform gallery. The farmer entry is visually available in the live preview and the two replacement assets are currently in their expected generation state, rather than permanently failed. The next validation pass will confirm the conversation dock after image generation finishes and exercise three scoped-routing cases: a farming question, a clearly fisherman-specific question from the farmer dock, and an unclassified request that should go to the open-field pathway.

The browser’s first interaction attempts scrolled to the farmer entry without surfacing the dock in the extracted accessibility tree. The production component has a direct button trigger and will receive a DOM-level interaction check after the latest build settles; this is being treated as a validation item rather than a visual conclusion.

The DOM-triggered validation successfully mounted the **AI for Farmers** dock with its farmer-specific greeting, scope note, close control, and accessible question field. The remaining behavior check is now limited to confirming the model-backed scope route outcomes.

The first in-scope farm question was incorrectly classified as an open-field request by the lightweight router model. The interaction confirms the fallback itself works, but it also shows that scope protection must add deterministic domain cues before using model classification. The next revision will force strong agricultural signals—such as tomato, leaves, crop, soil and harvest—to the farmer assistant, while still allowing clear cross-domain wording to route users to the appropriate designated panel.

After adding the deterministic cues and passing the updated unit suite, the live farmer dock was reset successfully and is ready for the corrected in-scope request check.

The corrected farm request remains inside the **AI for Farmers** dock after submission, which confirms the deterministic cue guard prevents the earlier premature open-field redirect. The response is still in its loading state at this observation point; the next check will wait for the generated farming guidance and then test an explicitly fishing-related cross-pathway redirect.

The endpoint completed with a server-side response-shape fallback rather than a routing failure. The model helper permits either a string or a structured text-part array; the route now normalizes both shapes, with dedicated unit coverage, before deciding whether an assistant response is empty.

The live conversation was reset after the normalization update, and the tomato request was resubmitted inside the farmer dock. The next observation will determine whether the normalized model output is now rendered correctly.

The model still returned an empty response after normalization. The discovered cause is a token-parameter mismatch: the current chat writer used a GPT-family model while the project helper forwards `max_tokens`; the fast Claude Haiku model accepts that helper contract directly. The response writer has been switched to Claude Haiku, while the low-cost routing classifier remains unchanged.

The response layer has now been replaced by a Gemini primary with a verified OpenRouter fallback. Both provider credentials passed lightweight health checks and the full TypeScript and test suite pass. A fresh live preview loaded successfully and the farmer conversation dock has been opened for response-path validation.

The in-scope tomato request has been accepted within the farmer dock under the new provider layer. The next check will confirm the completed response and reveal whether the Gemini primary or resilient fallback handled the request.

The Gemini-backed farmer conversation completed in the live preview with a scoped agricultural response. A fresh independent page load also renders the language trigger as a visible functional control in the masthead; the next pass will exercise the menu’s selector motion and a compact mobile viewport.

The masthead language control opens a compact animated menu with English, हिन्दी, and తెలుగు choices. Selecting हिन्दी visibly closes the menu, updates the trigger label, and confirms the changed chat preference through an in-product success message.

The farmer conversation has been reopened under the selected Hindi preference. The next live interaction will submit a clearly fishing-specific request to verify that the deterministic boundary guard redirects into the dedicated fishermen assistant instead of answering from the farmer scope.

The fishing-specific request was correctly redirected out of the farmer assistant and into **AI for Fishermen**. The dock title, pathway number, explanatory redirect message, and fishermen-specific greeting all updated in the live interface, confirming that a clear cross-domain request cannot drift into the wrong assistant.

The preview was reset cleanly and the farmer conversation was reopened to test the final routing branch: a community question that does not belong to any of the first eight designated domains.

The first open-field test exposed a routing edge case: the phrase “after school” caused a community-space question to drift into Rural Education & Skills. The deterministic boundary guard now prioritizes explicit neighbourhood and shared-community-space cues ahead of educational cues, with a regression test for the exact request.

The regression check now passes in the live preview: the shared-neighbourhood-space question immediately redirects from AI for Farmers to **09 / Your community, your challenge**, where the open-field greeting is shown. The three critical paths are now evidenced in the interface: an in-scope farmer answer, a farmers-to-fishermen redirect, and a farmers-to-open-field redirect.

After the initial-bundle optimization, the landing page loaded cleanly and a farmer pathway was opened to invoke the deferred chat module. The next interaction will verify that the on-demand conversation renderer is ready without affecting the first-load experience.

The deferred conversation renderer resolved successfully after opening the farmer pathway, presenting the scoped greeting and usable question field. The browser console contains no client-side errors after the optimized entry sequence. The production build also completes successfully with the rich chat renderer isolated in its own on-demand chunk, rather than loading on initial page view.

The final 375px render retains the intended editorial hierarchy and turns the signal index into a vertical sequence of large, reachable pathway bars. A compact-viewport interaction attempt correctly brought the farmer entry into view; the shared conversation event will be confirmed once more through its direct accessible control before finalizing the mobile checklist.

The farmer pathway’s accessible control was confirmed to mount the conversation dock and its usable text input after the responsive verification. The post-restart development log shows a clean current server startup with OAuth initialization and no missing-module entry. The earlier dotenv message was historical, from the interrupted full-stack upgrade before dependency installation.

Explicit performance and accessibility evidence is now recorded in code: the hero video uses `preload="metadata"` with a still-image poster, panel and editorial images are lazy-loaded, and the reduced-motion media query hides the video and uses the hero still as the fallback. In the connected Playwright session at an actual 375 × 812 viewport, the farmer pathway opened, accepted a crop-observation question, and rendered a response with zero console errors. A live response initially included text-only disease speculation, so a deterministic server-side farming guard was added. The final true-mobile response correctly states that text alone cannot confirm the cause, requests visible observations and a clear photo, and directs the visitor to a local agricultural extension professional; it contains no disease-name speculation.

The final runtime reduced-motion check was executed in the same connected 375 × 812 browser session. The browser reports the reduced-motion preference as active, the hero video’s computed display as `none`, and the hero background as the field-reference still. The runtime result also confirms that the hero video retains the matching poster URL and `metadata` preload setting. This closes the media-fallback validation with both source-code and browser-runtime evidence.

The immediate hero Signal Index now displays all nine scoped pathways on desktop without changing the editorial composition; at 375 × 812 it becomes a touch-scroll rail rather than a second dashboard. The desktop viewport check opened Farmers directly from that rail, mounted the conversation dock at the left edge (22px), muted the original hero context and removed its pointer interaction, then restored the context to full opacity after closing. The same open/close behavior was verified in the compact layout, while the index stayed available as orientation.

The final control audit found exactly nine hero-index buttons. Under `prefers-reduced-motion`, a Disaster Resilience chat opened with the hero context immediately hidden, the white cursor sphere suppressed, and the original context restored immediately on close. TypeScript checks, all 11 unit tests, and the production build complete successfully after the focused-takeover change; the build retains the existing non-blocking chunk-size advisory.

Exhaustive control coverage now confirms that all nine hero Signal Index controls open the matching title on both desktop and compact viewports. The compact check deliberately scrolled each item into the horizontal rail before activation, proving access through the complete mobile control sequence rather than only its initially visible items. Every result matched its expected pathway title: Farmers, Fishermen, Artisans, Micro-Entrepreneurs, Accessible Public Services, Persons with Disabilities, Rural Education & Skills, Disaster Resilience, and the open-field community pathway.

The supplied glass-flower video has replaced the desert media in the hero. Desktop inspection confirms that the flower occupies the central negative space without reducing contrast for the left editorial title or right Signal Index. At 375 × 812, the flower remains framed behind the editorial copy, with the call-to-action and horizontal pathway rail still legible and reachable.

The browser runtime confirms that the live `<video>` source is `/manus-storage/glass-flower_89bde4e6.mp4`, has reached ready state 4, retains `preload="metadata"`, and preserves the field-reference poster. With reduced motion enabled, the video is hidden and the hero uses the existing still-image fallback. TypeScript validation and all 11 unit tests pass after the media replacement.

The chartreuse system has been visually replaced with royal navy and controlled gold. On desktop, navy now establishes the field for the hero, pathway panels, conversational dock, and footer, while gold carries labels, numbering, and fine borders. Royal red is restricted to decisive hover and submission states, plus the small hero footnote markers. At 375 × 812, the navy call-to-action, gold title emphasis, and gold-numbered pathway rail retain clear separation against the glass-flower hero.

The compact runtime check closes the final video-media gap: at 375 × 812 the glass-flower source and field-reference poster are both present, and reduced motion hides the video while keeping the still fallback. The palette audit confirms the primary action resolves to royal blue `rgb(22, 75, 140)` with a gold border, hero-index numerals resolve to gold `rgb(224, 201, 142)`, and the settled pathway hover resolves to restrained royal red `rgb(132, 31, 62)`. TypeScript and all 11 unit tests pass after the palette refinement.

The hero video’s native playback contract has been verified live: it is muted, autoplaying, and has the loop flag enabled. When positioned 0.12 seconds before its 12.45-second end, it wrapped back to 0.58 seconds, remained unpaused, and never entered an ended state. Reduced motion still hides the video, preserving the existing non-animated fallback.

The first visual pass confirms the pathways now share one centered geometry rather than the former scattered offsets. Initial runtime inspection confirms ascending z-layers for the stacked cards and that the masthead language control is removed; an opened Farmers conversation exposes the chat-local language control and updates it to हिन्दी. The next audit will measure reverse-unwinding from static flow positions rather than sticky-adjusted viewport positions.

The corrected stack audit proves the intended first-in-last-out flow. Scrolling forward to the fourth panel pins panels 01–04 at a deliberate 20px, 26px, 32px, and 37px progression with ascending layers 20–23; panel 05 remains below the stack. Scrolling back toward panel 02 retains only panels 01–02 in the stack while panels 03–05 release to 449px, 1019px, and 1588px below the viewport, confirming reverse unwind. The masthead language control is absent; the dock selector opens and changes the active conversation preference to हिन्दी on desktop and తెలుగు on mobile. At 375px, panels retain sticky positioning with ascending top offsets and the chat language menu is visible and usable.

The final-card runway now gives panel 09 a dedicated settling interval before the pathway section yields. At desktop width, panel 09 settles at 66px over panel 08 at 60px while the next section starts at 875px—below the 720px viewport. Scrolling back releases panel 09 to 160px while panel 08 remains at 60px, preserving reverse order. The compact audit shows the same behavior: panel 09 settles at 40px over panel 08 at 37px before the next section begins, then releases to 120px as panel 08 reassumes its stack position.

The desktop visual pass confirms that the new isometric signal objects fill three otherwise quiet editorial zones without overlaying copy or the central card stack: a gold form supports the mission aside, a navy form enters near the pathway heading, and a red form marks the final-card runway. Their enlarged silhouettes remain behind content, preserve the royal palette, and avoid reintroducing cursor particles or unrelated visual noise.

The runtime audit confirms all three sculptures render as desktop-only, non-interactive elements with no animation: their measured desktop sizes are roughly 169–176px wide, `pointer-events` is `none`, and their animation name is `none`. All three are hidden at 375px to protect compact reading space, and their static nature is preserved under `prefers-reduced-motion`.

The first clearance-and-tilt audit confirms the mission and final-runway sculptures clear their neighboring text and panels, while card transforms alternate cleanly in sign on desktop and mobile and resolve to `none` under reduced motion. It also found that the navy sculpture’s bounding box still reaches the pathway heading, so it is being moved lower into the left-side stack whitespace before the refinement is finalized.

After repositioning, the clearance audit reports no overlap between any sculpture and the mission copy, mission aside, pathway heading, first pathway panel, or final pathway panel. The desktop cards now alternate between a restrained -0.48° and +0.48° rotation; the compact stack uses -0.32° and +0.32°; all tilts are removed under reduced motion. The final visual pass confirms that the objects sit in clipped edge whitespace and the counter-tilts add texture without compromising the clean stack order.

Post-tilt desktop stack checks retain the original behavior: panels 01–04 stack at 20px, 26px, 32px, and 37px in forward traversal, while panels 03–05 release below the viewport during reverse traversal. Panel 09 still settles at 66px above panel 08 at 60px while the next section remains below the viewport, then releases correctly as panel 08 resumes the lead stack position.

The compact post-tilt audit also remains stable: the leading pathway cards retain `sticky` positioning with 12px, 15.52px, and 19.04px anchors and ascending z-layers 20–22. Panel 09 settles at 40px above panel 08 at 37px before the following section appears, then releases to 120px while panel 08 returns to its 37px stack position. This confirms the alternating rotations did not compromise mobile stack order or the final-card handoff.

The pointer and playback audit confirms that a fine-pointer click applies the temporary sphere pop state and that it clears after 260ms while normal pointer visibility remains. When the pointer enters an open conversation dock, both visible and pop classes are removed and computed opacity reaches 0, so no cursor effect remains over chat content. The hero video now plays at 0.62×; reduced motion still hides both the hero video and cursor sphere.

Panels 04–09 now resolve to six dedicated managed image URLs for street economy, public services, disability access, rural learning, climate readiness, and open-field community problem framing. Panel imagery uses `saturate(0.82) brightness(0.8) contrast(1.04)` for a visibly brighter documentary treatment while retaining the left-side copy-safe composition specified in generation.

The configured ElevenLabs credential passes a live voice-list health test. A real guide-voice request from the Farmers conversation returns HTTP 200 and produces a ready, playing 13-second MP3 in the persistent chat audio element; the title-adjacent control correctly changes from “Listen” to “Stop” during playback. Browser request capture confirms that the chat language preference is sent through the server speech procedure for both English and हिन्दी, while the server keeps the provider credential private.

The compact chat audit confirms that the 375px layout preserves all guide controls without returning the language selector to the page masthead: it exposes “01 / Asha · Farm guide,” a title-adjacent “Listen to Asha speak” control, and the English chat-language control inside the open conversation dock. Prompt tests verify that Asha’s scoped system prompt identifies the AI guide persona while retaining explicit “not a real person” disclosure and farming safety instructions.

Telugu playback is now provider-compatible: ElevenLabs does not accept an explicit `te` language code for the selected multilingual model, so the server omits that unsupported metadata and allows multilingual script detection instead. The compact live audit captures a Telugu chat preference sent to the speech procedure, then receives a successful response with ready, actively playing audio and the button changed to “Stop Asha speaking.” English and Hindi retain explicit supported language metadata; all three paths keep user-facing provider errors contained to a retryable message.

The Hindi live audit confirms the selected हिन्दी preference reaches the speech request, which returns an active, ready audio element and a “Stop Asha speaking” control state. A controlled 500 response to the guide speech route visibly shows “Guide voice is unavailable right now. Please try again.” and returns the action to its enabled Listen state, confirming a clean retry path.

The desktop visual review confirms the original failure placeholders for Public Services, Disabilities, Rural Learning, and Disaster Resilience are replaced by brighter documentary images with the copy-safe left side still intact. The named guide label appears on every one of the nine panels. A direct DOM asset audit is inconclusive for offscreen lazy images because it does not scroll them into view; the final Open Field visual still requires a subsequent resolved-image confirmation before the full visual checklist can close.

The scroll-based lazy-asset audit confirms all nine pathway backgrounds and all nine guide portraits resolve at non-zero natural dimensions after entering view. Each background is 1920px wide, while the guide portraits resolve at 683px or 1280px wide; the final Open Field panel now carries its people-centered background rather than a placeholder. The compact full-page inspection confirms the vertical card stack retains readable white copy, right-side guide figures, and distinct documentary subjects through all nine panels. Image treatment was then lifted from 0.80 to 0.92 brightness with the existing scrim retained, so the practical detail reads more clearly without compromising copy contrast.

The final all-guide audit opens every one of the nine pathway conversations and confirms each dock displays its matching guide identity—Asha, Vikram, Meera, Farah, Nandini, Kiran, Ravi, Leela, and Saira—with a visible AI introduction. The original Farmers-to-Fishermen cross-scope question continues to redirect to “02 / Vikram · Coast guide,” preserving the intended scoped routing after persona changes. Together with desktop and compact full-page visual inspection, this confirms the brighter panels 04–09 retain recognizable subjects and readable overlay copy; Open Field visibly shows a resident group around a community-mapping moment.

Direct asset inspection confirms the final Open Field image depicts six community members collaboratively mapping a neighborhood around a table, with a dark copy-safe left field and visible gold paper markers. The Public Services image likewise shows a clearly lit service navigator helping an older resident with a phone and documents, retaining a high-contrast dark left text field. These source-level visuals, together with the desktop and compact render passes, support the late-panel review: subjects remain recognizable, the left overlay area is intentionally uncluttered, and the final 1.04 brightness/0.14 scrim treatment lifts the imagery without masking the copy.

Post-exposure render passes at 1280px and 375px confirm that panels 04–09 retain visible human or civic subjects on the right with the copy confined to the clean dark left field. The compact stack keeps the pathway titles, guide labels, and contrast hierarchy legible; the Open Field card shows the group-mapping composition at the final stack position rather than a placeholder or blank visual.

The final hero-media audit confirms the 12.47-second glass-flower video wraps from its endpoint to 0.04 seconds at the intended 0.62× playback rate without pausing or ending. Under reduced motion, the video remains hidden and the field-reference still becomes the hero background.

A fresh independent desktop review after the final exposure adjustment confirms that the late pathway imagery supports the grounded Indian civic context and the overall editorial hierarchy remains visually strong. It identifies broader future-brand refinements—especially a more distinctive signal-index system and more unified documentary grading—but does not flag subject invisibility or overlay legibility issues in panels 04–09.

The live DOM audit confirms all nine guide portrait elements currently receive an image response with nonzero dimensions. However, direct visual inspection of the existing `guide-leela-climate_c2190c92.png` and `guide-saira-community_f8a3d13f.png` managed assets in the browser shows the exact red-warning artwork and text “Image generation failed.” The current defect is therefore invalid generated image content rather than a missing DOM element or CSS visibility issue. A same-page canvas comparison was not used because the managed storage redirect taints the canvas across origins. No ElevenLabs request was made during this diagnosis.

The failed Leela portrait reference now points to the existing managed `guide-leela-resilience_820c3c75.jpg` file. Direct browser inspection confirms the replacement is a real 3000 × 5082 photograph of a woman working in a rice field, with no failed-generation artwork. This repair used a previously sourced local image and uploaded it as a managed web asset; it did not invoke an image-generation or voice service.

The failed Saira portrait reference now points to the existing managed `guide-saira-community_502c0ee5.jpg` file. Direct browser inspection confirms the replacement is a real 3000 × 4500 individual community-guide portrait, with no failed-generation artwork. Both repaired assets are distinct from all other guide portraits, and the accompanying regression test excludes the former failed Leela and Saira URLs. No voice request or image-generation call was made for the repair.

The focused local portrait-reference test passes. In the live page, after lazy loading completes, Leela’s updated guide element resolves to `guide-leela-resilience_820c3c75.jpg` at 3000 × 5082 and Saira’s resolves to `guide-saira-community_502c0ee5.jpg` at 3000 × 4500; both remain `display: block`. These checks exercised only static asset loading and did not open a chat, invoke ElevenLabs, or request image generation.

The photographic substitutions were removed after user feedback. Leela and Saira now render as two distinct embedded SVG art figures: Leela carries a field notebook alongside resilience motifs for sun, cloud, rain, and a leaf; Saira holds a community map with local network nodes and a dialogue mark. The page-level visual inspection confirms both are visible as illustrations in panels 08 and 09, with no “Image generation failed” artwork. The local TypeScript check and focused portrait-regression test pass; neither invokes the guide voice endpoint or image-generation services.

The final browser runtime check confirms panel 08 renders `panel-guide-illustration guide-illustration-resilience` and panel 09 renders `panel-guide-illustration guide-illustration-community`; neither contains a portrait image element or failed-image placeholder. Opening each conversation without clicking Listen confirms the expected title and enabled title-adjacent control—“Listen to Leela speak” and “Listen to Saira speak”—while the persistent audio element has a null source. No synthesis request or voice playback occurred during this check.

All temporary vector and photographic guide substitutes have now been superseded by the nine user-supplied illustrated figures. They are mapped by their matching subjects to Farmers, Fishermen, Artisans, Micro-Entrepreneurs, Accessible Public Services, Disability Access, Rural Learning, Disaster Resilience, and the Open Field community pathway. The focused mapping test passes. A live lazy-load audit confirms all nine supplied assets resolve as `display: block`, at 1122 × 1402, in their corresponding panel slots. The white-backed illustrations are deliberately framed as compact paper art plates on the right of each card, with eager loading for panels 08 and 09 so their supplied artwork does not appear as an empty plate during the final stack.

The final full-page desktop capture shows each supplied figure clearly as a consistent paper art plate aligned on the right of its corresponding card, retaining the panel title and scope in the clean dark left field. In a separate no-audio header check after this all-nine swap, the Leela dock displays “08 / Leela · Resilience guide,” the title “Disaster Resilience,” and an enabled “Listen to Leela speak” control while the audio element source remains null. The control was not pressed, so this confirms the layout and wiring without requesting ElevenLabs synthesis.

An independent full-page desktop review confirms that the supplied cutout figures are visually clear in their right-side paper-plate treatment. The reviewer recommends documentary imagery rather than isolated avatar cards as a broader visual-direction preference; however, the user explicitly supplied and requested these exact illustrated figures, so the user-selected one-to-one mapping is intentionally preserved. The review does not identify an image-generation failure, missing figure, or panel-text collision in the final treatment.

The user subsequently requested removal of all nine figures. The final figure-free desktop review confirms each pathway card now foregrounds its documentary panel image and clean left-side text hierarchy, with no guide art plate present. A runtime check records zero `.panel-guide-portrait` elements, nine retained guide-name labels, and an unchanged Leela conversation header with an inactive “Listen to Leela speak” control while the audio source remains null. No voice synthesis was requested during this confirmation.

The final full-page desktop capture provides inspectable visual evidence for the simplified stack: each card retains a readable dark copy field, a visible documentary image layer, and clear vertical separation without an empty art-plate region. The figure-free treatment keeps the cards visually balanced across the stack while preserving their distinct pathway subject matter.

A fresh independent review of the figure-free state characterizes the design as visually strong and recognizes the cinematic photography, numbered pathway system, and calm public-service tone as working elements. It offers optional brand-direction suggestions but does not flag an empty, unreadable, or visually unbalanced pathway card after the guide figures were removed. The user-approved royal-navy, red, and gold palette remains unchanged rather than adopting the reviewer’s conflicting older chartreuse recommendation.

Project documentation update: the root `README.md` now documents the complete current feature set, nine pathways, scoped routing, guide disclosure and safety boundaries, server-only multilingual guide voice, interaction design, accessibility, architecture, tRPC surface, managed-media conventions, local setup, secrets, validation evidence, and final figure-free card decision. A repository whitespace check passes.

Vercel deployment diagnosis: the original Vercel project was linked to the unrelated `bunny0arch/trinetras-agentic-hackathon` repository and served the Campus Placement application. A newly linked `AI-For-Public` Vercel project exposed that Vercel was treating the server entry as a static response rather than serving the Vite client. The minimal compatibility change adds a root default Express export for Vercel and emits the Vite client into the platform-required root `public/` build directory. Local type checking and the production build pass; no chat or voice provider requests were made during this work.

Vercel follow-up: the initial compatibility adapter alone still produced a raw server-source response. The routing is now made explicit with a root `vercel.json` that sets the build output to `public/`, retains the existing build command, routes managed `/manus-storage/*` paths into a narrow serverless adapter, and leaves client navigation to the Vite shell. The adapter reuses the existing Express routes; no chat, voice, routing, or safety implementation was changed. Type checking and a production build pass after the configuration update.

External Vercel verification is being performed against the user-created `ai-for-public-2` project under the `Trinetraas` team, connected to `bunny0arch/AI-For-Public`. The previous production deployments for `df3ff44` and `4d767ff` served raw bundled server source at the root. The explicit-configuration commit `60835d2` began a new production build and remains the deployment under verification; no conclusion is recorded until its status is ready and its assigned deployment URL is checked.

The `60835d2` deployment completed successfully and its specific Vercel URL rendered the full Collective Signal client shell with all nine pathway controls present. Its managed `/manus-storage/field-reference_86704408.jpg` request instead reached the SPA’s client 404, revealing that the catch-all Vercel rewrite was intercepting the more specific media route. The follow-up `0c6f296` deployment removes only that catch-all rewrite and remains under live verification.

The `0c6f296` deployment kept the static client shell working but routed a direct managed-media request to a platform 404. Its API adapter was present, but a direct `/api/health` request exposed the Vercel runtime import failures: first an ambiguous `../server` directory import, then extensionless local imports from the root app. The Vercel-only build now bundles the API adapter with esbuild so all existing local Express modules are self-contained in the deployed function. The Vercel-specific build and TypeScript check pass locally; generated `public/` and function artifacts were removed afterward and are not tracked source files.

The first deployed bundle was still shadowed by Vercel’s direct discovery of `api/[...path].ts`. The adapter source now lives at `server/vercelAdapter.ts`; `pnpm vercel-build` emits only its self-contained generated `api/[...path].js` function. The relocation passes TypeScript checking and produces the expected 53 kB function artifact locally, which is then removed along with the generated client output before committing.

Live Vercel logs on deployment `1de02e0` confirm the application shell remains available but its API function still resolves the unbundled `/var/task/server.js` entry and fails on `server/_core/oauth`. The relocated-source packaging correction was committed as `8d482ca2` and is the active deployment under verification. The Vercel project is `https://vercel.com/trinetraas/ai-for-public-2`, linked to `bunny0arch/AI-For-Public`; future checks must use the authenticated browser account that owns the `Trinetraas` team rather than the sandbox browser’s unrelated account.

In the fresh trusted 375 × 812 full-page compact capture, panels 04–09 remain individually visible in sequence after the final 1.04 brightness and 0.14 scrim adjustment. The street-economy, public-service, disability-access, rural-learning, and climate-resilience cards retain distinguishable people-centered documentary scenes, while their white titles, scope lines, guide labels, and action controls remain readable in their dark left-side overlay areas. The rendered panel 09 card visibly retains Saira’s group community-mapping scene rather than a placeholder, blank field, or text-only treatment. The independent reviewer characterizes the compact page as visually polished and does not identify any contrast, subject-visibility, or Open Field composition failure; its recommendations concern a prior, conflicting palette and index direction rather than this verified exposure pass.

On Vercel deployment `c680562`, an invalid `/api/trpc/invalid` request returned the expected structured tRPC `NOT_FOUND` response, proving that the generated API adapter now runs rather than falling through to static middleware. On deployment `cf245bd`, the dedicated managed-media route returned `Storage proxy not configured`, confirming that its function and rewrite now run correctly; the Vercel project itself has no environment variables. With the user’s approval, the existing hero video, logo, and pathway images were published as the public GitHub release `public-media-v1` and all rendered client references were changed to those release asset URLs. Local type checking, focused media and packaging tests, the Vercel-specific build, and a desktop landing-page visual check pass without invoking chat or voice providers. The Vercel AI chat and Listen controls still require user-owned provider credentials.

Final live verification on deployment `99f8b6a` (`https://ai-for-public-2-aii76yg0k-trinetraas.vercel.app/`) shows the complete Collective Signal client shell, active hero imagery, logo, and all nine pathway controls. The rendered page points its logo and all background imagery at the approved `public-media-v1` release assets rather than the unavailable Vercel storage proxy. A safe `/api/trpc/invalid` request returns the expected application-level structured tRPC `NOT_FOUND` JSON response, not a platform or module-resolution failure. No real chat request or ElevenLabs synthesis was made during verification.

The user explicitly accepted the Vercel deployment without adding provider credentials. Accordingly, the landing page, visual media, client interactions, download controls, and API routing are accepted as deployed; live AI chat and server-side ElevenLabs Listen remain intentionally unavailable in Vercel until the user later adds their own provider keys. No additional provider request was made.

Provider activation follow-up: user-approved Gemini and ElevenLabs credentials were added to Vercel Production and Preview, but two minimal in-scope text-chat attempts reached the Gemini endpoint and did not return a response. The application adapter and tRPC route remained healthy; no code-level deployment correction was required. The user then approved an OpenRouter fallback credential, which was added to the same Vercel environments and redeployed as `2QvodHwuxzGWit5vz4q6ipmbyLcp` at `https://ai-for-public-2-4aui04hzg-trinetraas.vercel.app/`. A single final Farmers question, “What are two low-risk steps to prepare soil for planting?”, returned a scoped practical soil-preparation response through the fallback. No ElevenLabs synthesis request was made during this activation sequence.

The subsequent production 500 occurred before any external provider call because the Vercel project does not have Manus-only router credentials. The chat router now deterministically sends ambiguous requests to the existing Open Field pathway when those internal credentials are unavailable, rather than failing. The client now shows an accessible “Checking your question” spinner with animated progress dots during requests and a retained-question error panel with Retry control if a response fails. Live verification on `https://ai-for-public-2-9ewyxalzo-trinetraas.vercel.app/` redirected an ambiguous Farmers question to Open Field without a 500, then returned a provider-backed scoped community-space response. No ElevenLabs synthesis was invoked.

Following feedback that a simple greeting should not move the visitor to another guide, routing now keeps greetings and lightweight conversational messages with the selected pathway. Explicit deterministic cross-pathway cues still redirect to the appropriate specialist. On deployment `7453d02` at `https://ai-for-public-2-l9weweih4-trinetraas.vercel.app/`, `Hi` in the Farmers conversation remained under Asha and returned an in-scope greeting response. No voice synthesis was invoked.

Guide redirect handoff refinement: a redirect now marks the current dock as busy, fades and pulls it left for 250 ms, unmounts it, then mounts the destination dock on the next animation frame. This prevents overlapping active guide panels while preserving the existing entrance motion. On deployment `24a8043` at `https://ai-for-public-2-e88xqcamq-trinetraas.vercel.app/`, a fisherman-specific sea-condition question submitted from Farmers transitioned to Vikram’s AI for Fishermen dock with the redirect explanation and guide greeting; only the destination dock remained after the handoff. No voice synthesis was invoked.

Enhanced handoff verification: deployment `6c72ee4` at `https://ai-for-public-2-hed69pt78-trinetraas.vercel.app/` renders a temporary “Switching to Vikram · AI for Fishermen” status and continuity notice during the transition. A Farmers-to-Fishermen handoff visibly retains the exact sea-condition question in Vikram’s conversation, followed by the redirect explanation and destination greeting; only one dock remains after the handoff. The compact layout uses a 210 ms vertical exit motion and safe-area-aware status placement, with a visual check at 375 × 812. No voice synthesis was invoked.
